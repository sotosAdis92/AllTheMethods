version: "3.8"
services:
  db:
    image: postgres:16.0
    ports:
      - 5432:5432
  
  backend:
    build: ./server/allTheMethods
    ports:
      - 8080:8080
    environment:
    
  
  frontend:
    build: ./clientside
    ports:
      - 5173:80

